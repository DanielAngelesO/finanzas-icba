Mejoras a UX de búsqueda:
Se abre con la lupita desde arriba (slide-in desde top), cubre toda la pantalla
Autofocus al abrirse (300ms delay para que la animación termine)
Resultados en tiempo real con cada keystroke (sin debounce — el dataset es local)
Contador de resultados: "3 resultados" actualizado en tiempo real
Cancelar: limpia input, cierra pantalla (sin animación molesta)
Muestra una opciona para el sistema de busqueda avanzada existente (quedan fuera de la pantalla principal).
Resultados:
Quita separacion de la linea con la fecha. Los resultados muestran la fecha junto al subtítulo (contexto extra)
Tap en resultado → abre detalle, luego regresa.
Filtros combinados:
Chips de tipo (Todos / Ingresos / Egresos / Transf.) disponibles dentro de la búsqueda, combinables con el texto.
Estos chips quedan fuera de la pantalla principal, solo se muestran en la busqueda.

